{
  "name": "digital-beat-maker",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:behavior": "playwright test tests/app-behavior.spec.js --reporter=line",
    "test:syntax": "node --check app.js && node --check qr-code.js",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}
